# Промпт для урока 3: Нейронные сети

### План урока и ключевые концепции:

1. **Интерактивное введение**:
```
Мини-игра "Создай свой нейрон":
- Входы: температура, дождь, выходной
- Задача: решить про пляж
- Цель: интуитивное понимание весов
```

2. **Базовая реализация нейрона**:
```python
class SimpleNeuron:
    def __init__(self, weights, bias):
        self.weights = weights
        self.bias = bias

    def activate(self, x):
        return 1 / (1 + np.exp(-x))  # Sigmoid

    def predict(self, inputs):
        sum_weighted = np.dot(inputs, self.weights) + self.bias
        return self.activate(sum_weighted)

# Пример использования
beach_neuron = SimpleNeuron(
    weights=[0.6, -0.7, 0.3],
    bias=-0.1
)
```

3. **Визуализация обучения**:
```python
def visualize_learning_process():
    fig = plt.figure(figsize=(12, 6))
    # Анимация процесса обучения
    plt.show()
```

4. **Послойное понимание**:
```python
def explain_layer(layer_number, data):
    """Визуализация активаций слоя"""
    plt.figure(figsize=(15, 5))
    # Визуализация
    plt.show()
```

5. **Интерактивный эксперимент**:
```python
@interact
def test_network(temperature=(0, 100),
                rain=(0, 100),
                is_weekend=(0, 1)):
    inputs = [temperature/100, rain/100, is_weekend]
    decision = beach_neuron.predict(inputs)

    print(f"Вероятность похода на пляж: {decision:.2%}")
```

### Специфические метрики успеха:
- Понимание принципа работы нейрона
- Умение менять поведение сети
- Понимание процесса обучения
- Видение послойной обработки
- Способность экспериментировать

### Возможные сложности:
1. **Сложность концепции**:
- Пошаговые объяснения
- Визуализации
- Интерактивные примеры

2. **Технические аспекты**:
- Готовые шаблоны кода
- Подробные комментарии
- Отладка вместе

### Практические задания:
1. Создание простого нейрона
2. Эксперименты с весами
3. Визуализация результатов
4. Многослойные эксперименты

### Связь с будущими темами:
- Переход к глубокому обучению
- Практические применения
- Современные архитектуры
