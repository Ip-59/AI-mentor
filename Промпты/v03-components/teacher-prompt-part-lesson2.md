# Промпт для урока 2: Основы машинного обучения

### План урока и ключевые концепции:

1. **Разминка и актуализация**:
```
Интерактивный эксперимент:
Последовательность чисел:
2 → 4
3 → 9
4 → 16
5 → ?

Цель: показать интуитивное понимание поиска закономерностей
```

2. **Введение в машинное обучение**:
```python
# Простой пример закономерности
import numpy as np

# Обучающие данные
X = np.array([2, 3, 4, 5])  # входные числа
y = np.array([4, 9, 16, 25])  # выходные числа

# Простая модель: y = ax^2
def simple_model(x, a):
    return a * x**2

# Проверка ошибки
def check_error(a):
    predictions = simple_model(X, a)
    error = np.mean((predictions - y)**2)
    return error
```

3. **Данные и признаки**:
```python
import pandas as pd

# Датасет о фруктах
fruits_data = pd.DataFrame({
    'вес': [150, 130, 180, 160, 170],
    'диаметр': [7, 6, 9, 8, 8],
    'цвет': ['красный', 'красный', 'желтый', 'красный', 'желтый'],
    'тип': ['яблоко', 'яблоко', 'банан', 'яблоко', 'банан']
})
```

4. **Три парадигмы ML**:
```
А. Обучение с учителем:
- Аналогия с обучением игре
- Примеры входных данных и ответов
- Обратная связь

Б. Обучение без учителя:
- Аналогия с сортировкой вещей
- Поиск закономерностей
- Группировка похожего

В. Обучение с подкреплением:
- Аналогия с велосипедом
- Проба и ошибка
- Награда за правильные действия
```

5. **Расширенное практическое задание**:
```python
# Погодный датасет
np.random.seed(42)
n_samples = 100

data = pd.DataFrame({
    'температура': np.random.uniform(15, 30, n_samples),
    'облачность': np.random.uniform(0, 100, n_samples)
})

# Создание меток
data['посещаемость'] = ['высокая' if (t > 22 and c < 60) else 'низкая'
                        for t, c in zip(data['температура'], data['облачность'])]

# Разделение данных
X = data[['температура', 'облачность']]
y = data['посещаемость']
```

6. **Ограничения ML**:
```
Ключевые ограничения:
- Необходимость данных
- Отсутствие причинности
- Проблемы экстраполяции
```

### Специфические метрики успеха:
- Понимание разницы между ML и классическим программированием
- Различение типов ML
- Успешные эксперименты с кодом
- Понимание ограничений ML
- Способность предложить тип ML для задачи

### Возможные сложности:
1. **Математическая сложность**:
- Фокус на интуитивном понимании
- Визуализации
- Практические примеры

2. **Путаница в типах ML**:
- Четкие аналогии
- Практические примеры
- Сравнительные таблицы

### Практические задания:
1. Модификация погодной модели
2. Эксп��рименты с параметрами
3. Анализ результатов
4. Предложение улучшений

### Связь со следующим уроком:
- Подготовка к изучению нейронных сетей
- Связь ML с глубоким обучением
